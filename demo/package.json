{
  "name": "tess2-rust-demo",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "bun run server.ts",
    "build": "bun run build.ts"
  },
  "devDependencies": {
    "@types/bun": "^1.3.8"
  }
}
